*.iml
.classpath
# Ignore .claude directories in subdirectories (e.g. module/.claude) but keep the root one,
# so shared rules are versioned while local settings stay out.
**/.claude
!/.claude
.claude/settings.local.json
claudedocs
.generated
.idea
.js
.jsweet
.project
.projects
.serena
.settings
.tmp
.ts
bin
**/jsweet_extension/**/*.class
schema-samples
target
website
