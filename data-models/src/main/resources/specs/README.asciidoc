Keep this folder synchronized with Apitomy Data Models repository (which is the canonical location for the specifications).

Feature TODOs:

- default values for properties
- validation for properties, e.g. positive integer property
